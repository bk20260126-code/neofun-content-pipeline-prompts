# mutation-directive.yaml — 성과 기반 지시
#
# `analyst` 스킬이 씁니다. `strategist`가 매번 시작할 때 읽습니다.
# analyst를 안 쓴다면 이 파일도 필요 없습니다. 파일명에서 .template을 빼고 쓰세요.

last_analyzed:     # YYYY-MM-DD
top_performer:     # 가장 반응 좋았던 글과 이유
bottom_performer:  # 가장 반응 없던 글과 이유

directives:
  hook:   # 다음 훅에 반영할 것
  theme:  # 다음 주제에 반영할 것
  cta:    # 다음 CTA에 반영할 것
