# STATE.md — 파이프라인 상태

> 여러 단계를 나눠서 돌릴 때만 필요합니다. 한 자리에서 다 끝낸다면 이 파일은 없어도 됩니다.
> 파일명을 `STATE.md`로 바꿔서 쓰세요.

```
PIPELINE_STATUS: IDLE
LAST_UPDATED:    —
```

## 상태 코드

| 코드 | 뜻 |
|---|---|
| `IDLE` | 진행 중인 작업 없음 |
| `BRIEF_READY` | 브리프 완성 |
| `DRAFT_READY` | 초안 완성 |
| `QA_FAIL_1` | QA 반려 1회 |
| `QA_FAIL_2` | QA 반려 2회 — **중단하고 사람이 판단** |
| `QA_PASSED` | QA 통과 |
| `COMPLETE` | 종료 |

## 회로 차단기

1. `QA_FAIL_2`에 도달하면 **자동 재시도를 멈춥니다.** AI가 스스로 통과할 때까지 고쳐 쓰게 두면 기준이 무너집니다.
2. 같은 단계에서 2시간 넘게 멈춰 있으면 중단하고 사람이 봅니다.

## 실행 로그

| 시각 | 단계 | 상태 | 산출 파일 | 메모 |
|---|---|---|---|---|
| — | — | — | — | — |
